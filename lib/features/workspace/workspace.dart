// Public entrypoints for the workspace feature.
//
// Implementation is split under `shell/`, `editor/`, `history/`, `collab/`,
// `search/`, and `templates/`. Import this library from outside the feature;
// do not re-export internal widgets from here.

export 'shell/workspace_page.dart';
export 'search/global_search_popup.dart';
