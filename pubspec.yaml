name: folio
description: "Folio is designed for users who value privacy, control, and productivity. Whether you're managing notes, organizing files, or leveraging local AI for content enhancement, Folio provides a seamless desktop-first experience with no cloud dependencies by default."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.0.4+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  cryptography: ^2.9.0
  path_provider: ^2.1.5
  local_auth: ^3.0.1
  uuid: ^4.5.3
  collection: ^1.19.1
  passkeys: ^2.18.0
  path: ^1.9.1
  shared_preferences: ^2.5.4
  google_fonts: ^8.0.2
  system_theme: ^3.1.2
  image_picker: ^1.1.2
  file_picker: ^11.0.2
  flutter_code_editor: ^0.3.5
  highlight: ^0.7.0
  diff_match_patch: ^0.4.1
  markdown: ^7.3.1
  flutter_markdown_plus: ^1.0.7
  archive: ^4.0.9
  video_player: ^2.11.1
  url_launcher: ^6.3.2
  emoji_picker_flutter: ^4.4.0
  syncfusion_flutter_pdfviewer: ^33.1.49
  hotkey_manager: ^0.2.3
  window_manager: ^0.5.1
  tray_manager: ^0.5.1
  syncfusion_flutter_pdf: ^33.1.49
  printing: ^5.14.3
  http: ^1.5.0
  # 10.x requiere win32 ^6; file_picker 11 aún usa win32 ^5 (incompatible en pub).
  package_info_plus: ^9.0.0
  pub_semver: ^2.2.0
  video_player_win: ^3.2.2
  webview_flutter: ^4.13.1
  webview_windows: ^0.4.0
  flutter_math_fork: ^0.7.4
  flutter_svg: ^2.2.0
  audioplayers: ^6.6.0
  record: ^6.2.0
  intl: ^0.20.2
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0
  cloud_functions: ^6.1.0
  flutter_quill: ^11.5.0
  cross_file: ^0.3.4+2
  desktop_drop: ^0.4.4
  super_clipboard: ^0.8.9
  firebase_analytics: ^12.3.0
  local_notifier: ^0.1.6
  idb_shim: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  msix: ^3.16.13

# passkeys_doctor (via passkeys) pins device_info_plus ^11.x; Syncfusion pdfviewer
# needs ^12.x. package_info_plus 10.x needs win32 ^6 but file_picker 11 still uses
# win32 ^5, so we stay on package_info_plus 9.x and override device_info to 12.x.
dependency_overrides:
  device_info_plus: ^12.4.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/icons/folio.ico
    - appLogos/jira.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Genera iconos de launcher en Android a partir del mismo arte que iOS.
flutter_launcher_icons:
  android: true
  ios: false
  image_path: ios/Runner/Assets.xcassets/AppIcon.appiconset/Icon-App-1024x1024@1x.png
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: ios/Runner/Assets.xcassets/AppIcon.appiconset/Icon-App-1024x1024@1x.png

# MSIX: con store:false el paquete se firma con cert. de prueba (instalable en tu PC).
# Para el MSIX que subes a Partner Center usa: dart run msix:create --store
# (ese artefacto no está pensado para doble clic local; Microsoft lo firma al publicar).
# msix_version: último segmento 0 según políticas de versión de la Store.
msix_config:
  display_name: Folio - Private Workspace
  publisher_display_name: Minealex Games
  # Estos 3 valores deben coincidir 1:1 con Partner Center → Product identity
  identity_name: MinealexGames.Folio-PrivateWorkspace
  publisher: CN=EFF3F915-1B14-4A6A-B8F6-8F232E1B13B0
  msix_version: 0.3.5.0
  logo_path: assets/icons/folio.ico
  capabilities: internetClient, microphone
  store: true
  install_certificate: true
